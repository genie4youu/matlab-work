# MATLAB / Simulink 빌드 산출물 — 소스가 아니다
slprj/
sccprj/
codegen/
*_ert_rtw/
*_grt_rtw/
*_sfun_rtw/
*_rtt/

# 자동 저장·백업
*.asv
*.autosave
*.slx.r*
*.mdl.r*
*.m~

# 컴파일 결과
*.mex*
*.o
*.obj
*.exe
*.dll
*.lib
*.elf
*.hex

# 캐시·로그
*.slxc
*.slreqx.bak
buildInfo.mat
rtwbuild.log
html/

# Simulink Coverage 리포트 — 생성물이다 (2026-09-07 추가)
#   첫 커밋에 48개가 그대로 들어갔다. `html/` 규칙이 폴더 이름만 보므로
#   `slcov_output/CoverageDetails/*.html` 은 걸리지 않았다.
slcov_output/
*.cvt

# Simulink 캐시 폴더
.SimulinkCache/

# OS
Thumbs.db
desktop.ini

# 저작 덤프 — emit_dump 가 재생성한다 (2026-09-08)
_덤프/

# 결과 폴더 안 Simulink 캐시 (build 스크립트가 fileGenControl 로 두는 것, 2026-09-18)
_slprj/
_덤프_독립/
